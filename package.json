{
  "name": "comap",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "stop": "npm --prefix frontend run stop",
    "dev": "npm --prefix frontend run dev",
    "dev:fresh": "npm --prefix frontend run dev:fresh",
    "build": "npm --prefix frontend run build",
    "serve": "npm --prefix frontend run serve"
  }
}
